# 30.5 NetworkCharacterDriver

**Commit:** `feat(phase-30): 30.5 NetworkCharacterDriver`
**Branch:** `feat/network-character-driver`

---

## Relevante Spezifikationen

Vor der Implementierung **PFLICHT** lesen — dies ist der kritischste Schritt der Phase:

| Dokument | Relevante Sektionen | Warum relevant |
|----------|---------------------|----------------|
| [Phase 30 Spec](../../specs/networking/Wiesenwischer_Phase30_FishNet_Native_Prediction_Migration.md) | "2. Motor-Integration (KRITISCH)", "3. Visual Separation", "30.5: NetworkCharacterDriver" | Tick-Flow-Reihenfolge, Pre/Post-Interpolation Gap, Replay-Safety |
| [FishNet Integration FULL](../../specs/networking/Wiesenwischer_FishNet_Networking_Integration_FULL.md) | "[Replicate]/[Reconcile] Pattern", "TimeManager.OnTick", "ReplicateState" | Komplette FishNet API: Wie Replicate/Reconcile funktioniert, state.IsReplayed(), Channel |
| [AAA Simulation vs Rendering FULL](../../specs/networking/Wiesenwischer_AAA_Simulation_vs_Rendering_FULL.md) | "Visual Interpolation", "Simulation != Rendering", "Motor Interpolation" | Warum Pre/Post-Interpolation manuell aufgerufen werden muss und NICHT im Replay |
| [MMO Prediction Advanced](../../specs/networking/Wiesenwischer_FishNet_MMO_Prediction_Advanced.md) | "Spectator Prediction", "One-Shot Inputs", "Reconcile-Replay" | Spectator-Prediction für Non-Owner, Input-Akkumulation zwischen Ticks |
| [Simulation Tick vs FixedUpdate](../../specs/networking/Wiesenwischer_GameKit_Simulation_Tick_vs_FixedUpdate.md) | "OnTick Flow", "AutoSimulation" | Warum CharacterMotorSystem.AutoSimulation=false und manuelles Simulate() |
| [Advanced Tips](../../specs/networking/Wiesenwischer_MMO_Networking_Advanced_Tips.md) | "Häufige Fehler", "Performance" | Bekannte Pitfalls bei [Replicate]-Implementierungen |

---

## Ziel

Die Kern-Komponente mit FishNet `[Replicate]`/`[Reconcile]` erstellen. `NetworkCharacterDriver` ersetzt `NetworkInputSync` + `NetworkStateSync` und ist der zentrale Netzwerk-Treiber fuer Character-Bewegung.

---

## Anweisungen

### 1. NetworkCharacterDriver Grundgeruest

Neue Datei: `Packages/Wiesenwischer.GameKit.Network.FishNet/Runtime/Core/NetworkCharacterDriver.cs`

```csharp
using FishNet.Object;
using FishNet.Object.Prediction;
using FishNet.Transporting;
using UnityEngine;
using Wiesenwischer.GameKit.CharacterController.Core;

namespace Wiesenwischer.GameKit.Network
{
    /// <summary>
    /// Treibt die Character-Simulation ueber FishNet's TimeManager.OnTick.
    /// Ersetzt NetworkInputSync + NetworkStateSync durch native [Replicate]/[Reconcile].
    /// </summary>
    [RequireComponent(typeof(PlayerController))]
    public class NetworkCharacterDriver : TickNetworkBehaviour, ISimulationDriver
    {
        // --- Referenzen ---
        private PlayerController _player;
        private CharacterMotor _motor;

        // --- ISimulationDriver ---
        public bool IsActive => IsSpawned;
        public float TickDelta => (float)TimeManager.TickDelta;
        public uint CurrentTick => TimeManager.Tick;

        // --- One-Shot Input Akkumulation ---
        private bool _jumpRequested;
        private bool _jumpCutRequested;
        private bool _resetVerticalRequested;
    }
}
```

### 2. Lifecycle: OnStartNetwork / OnStopNetwork

```csharp
public override void OnStartNetwork()
{
    base.OnStartNetwork();

    _player = GetComponent<PlayerController>();
    _motor = GetComponent<CharacterMotor>();

    // Motor-Simulation manuell steuern (kein FixedUpdate)
    CharacterMotorSystem.Settings.AutoSimulation = false;

    // TimeManager-Events registrieren
    TimeManager.OnTick += TimeManager_OnTick;
    TimeManager.OnPostTick += TimeManager_OnPostTick;
}

public override void OnStopNetwork()
{
    base.OnStopNetwork();

    // Zurueck zum Offline-Modus
    CharacterMotorSystem.Settings.AutoSimulation = true;

    TimeManager.OnTick -= TimeManager_OnTick;
    TimeManager.OnPostTick -= TimeManager_OnPostTick;
}
```

**ACHTUNG:** `AutoSimulation` ist global — betrifft ALLE Motoren. Das ist korrekt fuer MMO (alle Spieler netzwerk-getrieben). Bei Offline-NPCs muesste man das differenzieren — fuer Phase 30 akzeptabel.

### 3. Tick-Flow (KRITISCH — korrekte Reihenfolge)

```csharp
private void TimeManager_OnTick()
{
    // 1. Interpolation vorbereiten (NUR beim echten Tick, NICHT in Replay!)
    if (_motor != null)
        CharacterMotorSystem.PreSimulationInterpolationUpdate((float)TimeManager.TickDelta);

    // 2. Input sammeln + Replicate aufrufen
    BuildAndReplicate();

    // 3. Reconcile (Server-only, nach Replicate)
    CreateReconcile();
}

private void TimeManager_OnPostTick()
{
    // 4. Interpolation abschliessen (NUR beim echten Tick, NICHT in Replay!)
    if (_motor != null)
        CharacterMotorSystem.PostSimulationInterpolationUpdate((float)TimeManager.TickDelta);
}
```

**Warum Pre/Post ausserhalb von [Replicate]?**
`[Replicate]` wird auch waehrend Replay aufgerufen (mehrere Ticks hintereinander). Pre/Post-Interpolation darf nur beim echten Tick laufen, sonst werden die Interpolations-Timestamps verfaelscht.

### 4. Input sammeln + BuildReplicateData

```csharp
private void BuildAndReplicate()
{
    MoveReplicateData input = default;

    if (IsOwner)
    {
        input = BuildReplicateData();
        // One-Shot Flags zuruecksetzen nach Einlesen
        _jumpRequested = false;
        _jumpCutRequested = false;
        _resetVerticalRequested = false;
    }

    PerformReplicate(input);
}

private MoveReplicateData BuildReplicateData()
{
    var reusable = _player.ReusableData;
    var inputProvider = _player.InputProvider;

    return new MoveReplicateData
    {
        MoveDirection = inputProvider.MoveInput,
        CameraYaw = _player.CameraYaw,  // KRITISCH: behebt den Yaw=0 Bug
        CharacterRotation = _player.transform.eulerAngles.y,
        Buttons = BuildControllerButtons(),
        SpeedModifier = reusable.SpeedModifier,
        JumpRequested = _jumpRequested,
        JumpCutRequested = _jumpCutRequested,
        ResetVerticalRequested = _resetVerticalRequested,
    };
}

private ControllerButtons BuildControllerButtons()
{
    var buttons = ControllerButtons.None;
    var input = _player.InputProvider;

    if (input.SprintHeld) buttons |= ControllerButtons.Sprint;
    if (input.CrouchTogglePressed) buttons |= ControllerButtons.Crouch;
    if (_player.ReusableData.ShouldWalk) buttons |= ControllerButtons.Walk;

    return buttons;
}
```

### 5. [Replicate] Methode

```csharp
[Replicate]
private void PerformReplicate(MoveReplicateData input, ReplicateState state,
    Channel channel = Channel.Unreliable)
{
    // Waehrend Created-State (kein Input vorhanden fuer Spectator): letzten Input wiederholen
    // FishNet setzt state automatisch auf Created fuer Non-Owner-Future-Ticks

    // Input auf ReusableData/Locomotion setzen
    ApplyInputToPlayer(input);

    // Simulation ausfuehren
    _player.SimulateTick((float)TimeManager.TickDelta);

    // Motor simulieren (KCC UpdateVelocity/UpdateRotation Callbacks)
    if (_motor != null)
    {
        CharacterMotorSystem.Simulate(
            (float)TimeManager.TickDelta,
            new System.Collections.Generic.List<CharacterMotor> { _motor }
        );
    }
}

private void ApplyInputToPlayer(MoveReplicateData input)
{
    var reusable = _player.ReusableData;

    reusable.MoveInput = input.MoveDirection;
    reusable.CameraYaw = input.CameraYaw;
    reusable.SpeedModifier = input.SpeedModifier;

    // One-Shot Events
    if (input.JumpRequested) reusable.JumpPressed = true;
    if (input.JumpCutRequested) reusable.JumpCutRequested = true;
    if (input.ResetVerticalRequested) reusable.ResetVerticalRequested = true;

    // Button-States
    reusable.SprintHeld = input.Buttons.HasFlag(ControllerButtons.Sprint);
    reusable.ShouldWalk = input.Buttons.HasFlag(ControllerButtons.Walk);
}
```

### 6. [Reconcile] Methode

```csharp
private void CreateReconcile()
{
    if (!IsServerStarted) return;

    var motor = _motor;
    var reusable = _player.ReusableData;

    var data = new CharacterReconcileData
    {
        Position = motor.TransientPosition,
        Rotation = motor.TransientRotation.eulerAngles.y,
        Velocity = reusable.CurrentVelocity,
        VerticalVelocity = reusable.VerticalVelocity,
        IsGrounded = reusable.IsGrounded,
        IsCrouching = reusable.IsCrouching,
        ShouldWalk = reusable.ShouldWalk,
        MovementStateIndex = _player.CurrentMovementStateIndex,
    };

    PerformReconcile(data);
}

[Reconcile]
private void PerformReconcile(CharacterReconcileData data, Channel channel = Channel.Unreliable)
{
    // Position/Rotation auf Motor setzen (Physik-State)
    _motor.SetPositionAndRotation(
        data.Position,
        Quaternion.Euler(0f, data.Rotation, 0f)
    );

    // Character-State wiederherstellen
    var reusable = _player.ReusableData;
    reusable.CurrentVelocity = data.Velocity;
    reusable.VerticalVelocity = data.VerticalVelocity;
    reusable.IsGrounded = data.IsGrounded;
    reusable.IsCrouching = data.IsCrouching;
    reusable.ShouldWalk = data.ShouldWalk;

    // StateMachine State wiederherstellen (falls noetig)
    _player.RestoreMovementState(data.MovementStateIndex);
}
```

### 7. One-Shot Input Akkumulation (Update)

Zwischen Ticks koennen One-Shot-Events auftreten (Jump-Press). Diese muessen bis zum naechsten Tick akkumuliert werden:

```csharp
private void Update()
{
    if (!IsOwner) return;

    // One-Shot Inputs akkumulieren (gehen nicht verloren zwischen Ticks)
    var input = _player.InputProvider;

    if (input.JumpPressed) _jumpRequested = true;
    if (input.JumpReleased) _jumpCutRequested = true;
    // ResetVertical wird intern von der StateMachine gesetzt
}
```

### 8. Hinweise zur Motor-Interpolation

- `CustomInterpolationUpdate()` in `CharacterMotorSystem.LateUpdate()` laeuft **weiterhin automatisch**
- Sie nutzt die von `PostSimulationInterpolationUpdate()` gesetzten Timestamps
- `Transform.position` (was die Kamera sieht) wird in LateUpdate auf den interpolierten Wert gesetzt
- Die Kamera folgt `Transform.position` ueber `CameraAnchor.FollowTarget` → korrekt

---

## Verifikation

- [ ] `NetworkCharacterDriver` extends `TickNetworkBehaviour`
- [ ] `NetworkCharacterDriver` implementiert `ISimulationDriver`
- [ ] `OnStartNetwork()` setzt `AutoSimulation = false`
- [ ] `OnStopNetwork()` setzt `AutoSimulation = true`
- [ ] `TimeManager_OnTick()` ruft Pre-Interpolation → Replicate → Reconcile auf
- [ ] `TimeManager_OnPostTick()` ruft Post-Interpolation auf
- [ ] `[Replicate]` simuliert NUR (keine Pre/Post-Interpolation)
- [ ] `[Reconcile]` stellt Position/Rotation/Velocity/State wieder her
- [ ] `CameraYaw` wird in `MoveReplicateData` korrekt gesetzt
- [ ] One-Shot Inputs (Jump) werden zwischen Ticks akkumuliert
- [ ] Kompilierung fehlerfrei

---

## Erwartete Dateien

**Neu:**
```
Packages/Wiesenwischer.GameKit.Network.FishNet/Runtime/Core/
└── NetworkCharacterDriver.cs
```

---

## Hinweise

- `PerformReplicate()` wird auch waehrend Reconcile-Replay aufgerufen — deshalb KEINE Pre/Post-Interpolation darin
- `ReplicateState.IsReplayed()` kann fuer zusaetzliche Replay-Guards genutzt werden
- FishNet's `PredictionManager` bietet Smoother-Optionen fuer spaetere Optimierung (nicht Phase 30)
- `Dispose()` in `MoveReplicateData` wird von FishNet nach Verarbeitung aufgerufen

---

→ Naechster Schritt: [30.6 PlayerController + NetworkPlayer Integration](30.6-network-integration.md)
