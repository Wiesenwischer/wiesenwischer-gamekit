# 29.2 ICameraInputStrategy + Pipeline Refactoring

> **Commit:** `feat(phase-29): 29.2 ICameraInputStrategy + Pipeline Refactoring`
> **Branch:** `feat/input-strategy-refactoring` (vom Integration-Branch)

---

## Ziel

Das **if/else auf OrbitActivation** in `CameraInputPipeline.DetermineOrbitMode()` durch ein **Strategy Pattern** ersetzen. Mode-spezifische Logik wird in austauschbare `ICameraInputStrategy`-Implementierungen extrahiert. Neue Camera-Modi (Mount, PhotoMode) erfordern dann nur eine neue Strategy-Klasse.

---

## Neue Dateien

### 1. ICameraInputStrategy.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.Camera.Core/Runtime/Input/ICameraInputStrategy.cs`

```csharp
using UnityEngine;

namespace Wiesenwischer.GameKit.Camera
{
    /// <summary>
    /// Strategie für mode-spezifische Input-Interpretation.
    /// Wird vom CameraPreset bestimmt und in die Pipeline injiziert.
    /// Ersetzt das if/else auf OrbitActivation in DetermineOrbitMode().
    /// </summary>
    public interface ICameraInputStrategy
    {
        /// <summary>Bestimmt OrbitMode basierend auf Button-State.</summary>
        CameraOrbitMode DetermineOrbitMode(bool freeLookHeld, bool steerHeld, bool isGamepad);

        /// <summary>Soll Look-Input gelesen werden?</summary>
        bool ShouldReadLookInput(CameraOrbitMode mode);

        /// <summary>Initialer Cursor-State beim Aktivieren der Strategy.</summary>
        CursorLockMode InitialCursorState { get; }

        /// <summary>Cursor-State basierend auf aktuellem OrbitMode.</summary>
        CursorLockMode GetCursorState(CameraOrbitMode mode);
    }
}
```

### 2. AlwaysOnInputStrategy.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.Camera.Core/Runtime/Input/AlwaysOnInputStrategy.cs`

```csharp
using UnityEngine;

namespace Wiesenwischer.GameKit.Camera
{
    /// <summary>
    /// ActionCombat-Mode: Maus steuert immer die Kamera.
    /// Cursor ist immer gelockt, immer FreeOrbit.
    /// </summary>
    public class AlwaysOnInputStrategy : ICameraInputStrategy
    {
        public CameraOrbitMode DetermineOrbitMode(bool freeLookHeld, bool steerHeld, bool isGamepad)
            => CameraOrbitMode.FreeOrbit;

        public bool ShouldReadLookInput(CameraOrbitMode mode) => true;

        public CursorLockMode InitialCursorState => CursorLockMode.Locked;

        public CursorLockMode GetCursorState(CameraOrbitMode mode) => CursorLockMode.Locked;
    }
}
```

### 3. ButtonActivatedInputStrategy.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.Camera.Core/Runtime/Input/ButtonActivatedInputStrategy.cs`

```csharp
using UnityEngine;

namespace Wiesenwischer.GameKit.Camera
{
    /// <summary>
    /// ClassicMMO-Mode: LMB = FreeOrbit, RMB = SteerOrbit.
    /// Ohne Button = kein Orbit, Cursor frei.
    /// Gamepad = immer FreeOrbit (Sticks steuern Kamera).
    /// </summary>
    public class ButtonActivatedInputStrategy : ICameraInputStrategy
    {
        public CameraOrbitMode DetermineOrbitMode(bool freeLookHeld, bool steerHeld, bool isGamepad)
        {
            if (isGamepad) return CameraOrbitMode.FreeOrbit;
            if (steerHeld) return CameraOrbitMode.SteerOrbit;
            if (freeLookHeld) return CameraOrbitMode.FreeOrbit;
            return CameraOrbitMode.None;
        }

        public bool ShouldReadLookInput(CameraOrbitMode mode)
            => mode != CameraOrbitMode.None;

        public CursorLockMode InitialCursorState => CursorLockMode.None;

        public CursorLockMode GetCursorState(CameraOrbitMode mode)
            => mode != CameraOrbitMode.None ? CursorLockMode.Locked : CursorLockMode.None;
    }
}
```

---

## Bestehende Datei modifizieren

### CameraInputPipeline.cs refactoren

**Pfad:** `Packages/Wiesenwischer.GameKit.Camera.Core/Runtime/Input/CameraInputPipeline.cs`

**Änderungen:**

#### 1. Neues Feld + Property

```csharp
private ICameraInputStrategy _strategy = new AlwaysOnInputStrategy();

/// <summary>Strategy austauschen (wird von CameraBrain bei SetPreset aufgerufen).</summary>
public ICameraInputStrategy Strategy
{
    get => _strategy;
    set
    {
        _strategy = value ?? new AlwaysOnInputStrategy();
        UpdateCursorState(_strategy.InitialCursorState);
    }
}
```

#### 2. DetermineOrbitMode() vereinfachen

**Vorher (if/else):**
```csharp
private CameraOrbitMode DetermineOrbitMode()
{
    if (_orbitActivation == OrbitActivation.AlwaysOn)
        return CameraOrbitMode.FreeOrbit;
    if (_isGamepad) return CameraOrbitMode.FreeOrbit;
    // ... etc.
}
```

**Nachher (Strategy):**
```csharp
private CameraOrbitMode DetermineOrbitMode()
{
    bool freeLookHeld = _freeLookAction?.IsPressed() ?? false;
    bool steerHeld = _steerAction?.IsPressed() ?? false;
    return _strategy.DetermineOrbitMode(freeLookHeld, steerHeld, _isGamepad);
}
```

#### 3. ProcessInput() anpassen

```csharp
public CameraInputState ProcessInput(float deltaTime)
{
    CameraOrbitMode orbitMode = DetermineOrbitMode();
    UpdateCursorState(_strategy.GetCursorState(orbitMode));

    Vector2 rawLook = Vector2.zero;
    if (_strategy.ShouldReadLookInput(orbitMode))
        rawLook = _lookAction?.ReadValue<Vector2>() ?? Vector2.zero;

    // ... Rest der Pipeline (Deadzone, Acceleration, Smoothing) bleibt unverändert
}
```

#### 4. OnEnable() anpassen

Initial Cursor-State über Strategy statt hartkodiert:

```csharp
private void OnEnable()
{
    // ... Input-Actions initialisieren (bleibt) ...
    UpdateCursorState(_strategy.InitialCursorState);
}
```

#### 5. OrbitActivationMode Property — Deprecation oder Entfernung

Das bestehende `OrbitActivationMode` Property kann entfernt werden, da die Strategy das jetzt steuert. Stattdessen wird `Strategy` von außen gesetzt (durch CameraBrain.SetPreset).

**Option A (empfohlen):** Property beibehalten, aber intern Strategy setzen:
```csharp
public OrbitActivation OrbitActivationMode
{
    get => _orbitActivation;
    set
    {
        _orbitActivation = value;
        Strategy = value switch
        {
            OrbitActivation.AlwaysOn => new AlwaysOnInputStrategy(),
            OrbitActivation.ButtonActivated => new ButtonActivatedInputStrategy(),
            _ => new AlwaysOnInputStrategy()
        };
    }
}
```

**Begründung:** Bestehender Code, der `OrbitActivationMode` setzt (CameraBrain.SetPreset), funktioniert weiterhin. Strategy und OrbitActivationMode bleiben synchron.

---

## Anweisungen

### Schritt 1: ICameraInputStrategy.cs erstellen

1. Erstelle die Datei im `Input/`-Unterordner von Camera.Core Runtime
2. Namespace: `Wiesenwischer.GameKit.Camera`

### Schritt 2: AlwaysOnInputStrategy.cs erstellen

1. Erstelle im selben `Input/`-Ordner
2. Triviale Implementierung — immer FreeOrbit, immer Locked

### Schritt 3: ButtonActivatedInputStrategy.cs erstellen

1. Erstelle im selben `Input/`-Ordner
2. Enthält die bisherige Button-Check-Logik aus DetermineOrbitMode()

### Schritt 4: CameraInputPipeline.cs refactoren

1. `_strategy` Feld + `Strategy` Property hinzufügen
2. `DetermineOrbitMode()` auf Strategy delegieren
3. `ProcessInput()` — ShouldReadLookInput + GetCursorState über Strategy
4. `OnEnable()` — Initial Cursor über Strategy
5. `OrbitActivationMode` Setter anpassen, damit er die Strategy setzt
6. Private Felder entfernen, die jetzt die Strategy kapselt (z.B. button-check Logik in ProcessInput)

**Wichtig:** Die öffentliche API (`ProcessInput()` Rückgabetyp, `CurrentInput` Property) bleibt identisch. Nur die interne Logik wird umstrukturiert.

---

## Verifikations-Checkliste

- [ ] `ICameraInputStrategy.cs` existiert in `Camera.Core/Runtime/Input/`
- [ ] `AlwaysOnInputStrategy.cs` existiert in `Camera.Core/Runtime/Input/`
- [ ] `ButtonActivatedInputStrategy.cs` existiert in `Camera.Core/Runtime/Input/`
- [ ] `CameraInputPipeline` hat `Strategy` Property
- [ ] `DetermineOrbitMode()` delegiert an Strategy
- [ ] `ProcessInput()` nutzt `ShouldReadLookInput()` und `GetCursorState()`
- [ ] `OnEnable()` nutzt `InitialCursorState`
- [ ] `OrbitActivationMode` Setter setzt auch die Strategy
- [ ] Verhalten ist identisch zum bisherigen (kein funktionaler Unterschied)
- [ ] Projekt kompiliert fehlerfrei
- [ ] Bestehende Tests laufen weiterhin grün

---

## Erwartete Dateien nach diesem Schritt

```
Packages/Wiesenwischer.GameKit.Camera.Core/Runtime/Input/
├── CameraInputPipeline.cs           (REFACTORED)
├── ICameraInputStrategy.cs          (NEU)
├── AlwaysOnInputStrategy.cs         (NEU)
└── ButtonActivatedInputStrategy.cs  (NEU)
```

---

## Nächster Schritt

→ [29.3 CameraOrientationProvider Implementierung](29.3-camera-orientation-provider.md)
