# 29.8 Unit Tests

> **Commit:** `test(phase-29): 29.8 Unit Tests für Orientation/Facing System`
> **Branch:** `test/orientation-facing-tests` (vom Integration-Branch)

---

## Ziel

Umfassende **Unit Tests** für alle neuen und modifizierten Komponenten der Phase 29.

---

## Test-Dateien

### 1. OrientationFacingInterfaceTests.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.CharacterController.Core/Tests/Runtime/OrientationFacingInterfaceTests.cs`

**Testfälle:**

```csharp
[TestFixture]
public class OrientationFacingInterfaceTests
{
    // --- FacingMode Default ---
    [Test] FacingMode_DefaultValue_IsMovementDirection()
    // LocomotionInput mit Default FacingMode = MovementDirection (Wert 0)

    // --- LocomotionInput Rückwärtskompatibilität ---
    [Test] LocomotionInput_WithoutNewFields_DefaultsToMovementDirection()
    // Erstelle LocomotionInput nur mit alten Feldern → FacingMode = MovementDirection
}
```

### 2. InputStrategyTests.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.Camera.Core/Tests/Runtime/InputStrategyTests.cs`

**Testfälle:**

```csharp
[TestFixture]
public class InputStrategyTests
{
    // --- AlwaysOnInputStrategy ---
    [Test] AlwaysOn_DetermineOrbitMode_AlwaysReturnsFreeOrbit()
    [Test] AlwaysOn_ShouldReadLookInput_AlwaysTrue()
    [Test] AlwaysOn_InitialCursorState_IsLocked()
    [Test] AlwaysOn_GetCursorState_AlwaysLocked()

    // --- ButtonActivatedInputStrategy ---
    [Test] ButtonActivated_NoButtons_ReturnsNone()
    [Test] ButtonActivated_FreeLookHeld_ReturnsFreeOrbit()
    [Test] ButtonActivated_SteerHeld_ReturnsSteerOrbit()
    [Test] ButtonActivated_BothHeld_SteerTakesPriority()
    [Test] ButtonActivated_Gamepad_ReturnsFreeOrbit()
    [Test] ButtonActivated_ShouldReadLookInput_FalseForNone()
    [Test] ButtonActivated_ShouldReadLookInput_TrueForFreeOrbit()
    [Test] ButtonActivated_ShouldReadLookInput_TrueForSteerOrbit()
    [Test] ButtonActivated_InitialCursorState_IsNone()
    [Test] ButtonActivated_GetCursorState_LockedForOrbit_NoneForNone()
}
```

### 3. CameraOrientationProviderTests.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.Camera.Core/Tests/Runtime/CameraOrientationProviderTests.cs`

**Testfälle:**

```csharp
[TestFixture]
public class CameraOrientationProviderTests
{
    // Setup: CameraBrain-Mock oder realer CameraBrain mit Test-Config

    // --- Movement Frame ---
    [Test] AlwaysOn_GetMovementForward_ReturnsCameraForward()
    [Test] ButtonActivated_None_GetMovementForward_ReturnsCharacterForward()
    [Test] ButtonActivated_FreeOrbit_GetMovementForward_ReturnsCharacterForward()
    [Test] ButtonActivated_SteerOrbit_GetMovementForward_ReturnsCameraForward()

    // --- Movement Right ---
    [Test] GetMovementRight_IsPerpendicularToForward()
    [Test] GetMovementRight_IsHorizontal()

    // --- Facing Mode ---
    [Test] AlwaysOn_GetFacingMode_ReturnsMovementDirection()
    [Test] ButtonActivated_None_GetFacingMode_ReturnsMovementDirection()
    [Test] ButtonActivated_FreeOrbit_GetFacingMode_ReturnsMovementDirection()
    [Test] ButtonActivated_SteerOrbit_GetFacingMode_ReturnsCameraForward()

    // --- Facing Direction ---
    [Test] GetFacingDirection_ReturnsCameraBrainForward()
}
```

**Hinweis:** Tests benötigen einen CameraBrain mit konfigurierbarem State. Entweder:
- Realer CameraBrain auf Test-GameObject (mit minimal Config)
- Oder Reflection/Mocking für CameraBrain-Properties

### 4. AnimationSpaceConverterTests.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.CharacterController.Core/Tests/Runtime/AnimationSpaceConverterTests.cs`

**Testfälle:**

```csharp
[TestFixture]
public class AnimationSpaceConverterTests
{
    // --- WorldToLocal ---
    [Test] WorldToLocal_ForwardMovement_ReturnsPositiveZ()
    [Test] WorldToLocal_RightMovement_ReturnsPositiveX()
    [Test] WorldToLocal_BackwardMovement_ReturnsNegativeZ()
    [Test] WorldToLocal_LeftMovement_ReturnsNegativeX()
    [Test] WorldToLocal_ZeroInput_ReturnsZero()
    [Test] WorldToLocal_RotatedCharacter_CorrectlyTransforms()

    // --- GetTurnAngle ---
    [Test] GetTurnAngle_SameDirection_ReturnsZero()
    [Test] GetTurnAngle_RightTurn_ReturnsPositive()
    [Test] GetTurnAngle_LeftTurn_ReturnsNegative()
    [Test] GetTurnAngle_180Turn_Returns180OrMinus180()
    [Test] GetTurnAngle_ZeroInput_ReturnsZero()
}
```

### 5. CharacterLocomotion Rotation Tests (Erweiterung)

**Pfad:** Bestehende Test-Datei erweitern oder neue Datei:
`Packages/Wiesenwischer.GameKit.CharacterController.Core/Tests/Runtime/Movement/LocomotionRotationTests.cs`

**Testfälle:**

```csharp
[TestFixture]
public class LocomotionRotationTests
{
    // --- FacingMode.MovementDirection ---
    [Test] MovementDirection_RotatesTowardVelocity()
    [Test] MovementDirection_NoMovement_NoRotation()

    // --- FacingMode.CameraForward ---
    [Test] CameraForward_RotatesTowardFacingDirection()
    [Test] CameraForward_NoFacingDirection_NoRotation()

    // --- FacingMode.None ---
    [Test] None_KeepsCurrentRotation()

    // --- Rückwärtskompatibilität ---
    [Test] DefaultFacingMode_BehavesLikeMovementDirection()
}
```

---

## Anweisungen

### Schritt 1: Test-Dateien erstellen

1. Erstelle alle 5 Test-Dateien (oder 4 + Erweiterung)
2. Nutze `[TestFixture]` und `[Test]` Attribute (NUnit)
3. Setup/Teardown für GameObjects (`new GameObject()` + `AddComponent<>()`)

### Schritt 2: Test-Utilities

Falls nötig: Helper-Methoden für CameraBrain-Setup in Tests. z.B.:
```csharp
private CameraBrain CreateTestBrain(OrbitActivation activation, CameraOrbitMode mode)
```

### Schritt 3: Tests ausführen

```bash
# Unity Test Runner (falls CLI verfügbar)
# Oder: Unity Editor → Window → General → Test Runner → Run All
```

### Schritt 4: Bestehende Tests prüfen

Alle bestehenden Tests müssen weiterhin grün sein — besonders:
- Phase-28 CameraPreset/Intent Tests
- Phase-4 Locomotion Tests
- Phase-8 IK Tests

---

## Verifikations-Checkliste

- [ ] `OrientationFacingInterfaceTests.cs` existiert und alle Tests grün
- [ ] `InputStrategyTests.cs` existiert und alle Tests grün
- [ ] `CameraOrientationProviderTests.cs` existiert und alle Tests grün
- [ ] `AnimationSpaceConverterTests.cs` existiert und alle Tests grün
- [ ] Locomotion Rotation Tests existieren und alle Tests grün
- [ ] Alle bestehenden Tests weiterhin grün
- [ ] Projekt kompiliert fehlerfrei

---

## Erwartete Dateien nach diesem Schritt

```
Packages/Wiesenwischer.GameKit.CharacterController.Core/Tests/Runtime/
├── OrientationFacingInterfaceTests.cs     (NEU)
├── AnimationSpaceConverterTests.cs        (NEU)
└── Movement/
    └── LocomotionRotationTests.cs         (NEU oder ERWEITERT)

Packages/Wiesenwischer.GameKit.Camera.Core/Tests/Runtime/
├── InputStrategyTests.cs                  (NEU)
└── CameraOrientationProviderTests.cs      (NEU)
```

---

## Nächster Schritt

→ [29.9 Play Mode Verifikation](29.9-play-mode-verifikation.md)
