# 29.1 IOrientationProvider + IFacingProvider Interfaces

> **Commit:** `feat(phase-29): 29.1 IOrientationProvider + IFacingProvider Interfaces`
> **Branch:** `feat/orientation-facing-interfaces` (vom Integration-Branch)

---

## Ziel

Die **drei neuen Abstraktionen** in CC.Core definieren, die Movement Frame-Space und Character Facing sauber trennen:

1. **`IOrientationProvider`** — In welchem Koordinatensystem wird WASD interpretiert?
2. **`IFacingProvider`** — Wohin soll der Character rotieren?
3. **`FacingMode`** Enum — Was treibt die Character-Rotation an?

Zusätzlich **`LocomotionInput`** um die Felder `FacingMode` und `FacingDirection` erweitern.

**Warum getrennte Interfaces?** Orientation (Frame-Space) und Facing sind unabhängige Konzepte. In ActionCombat ist der Frame-Space immer Camera, aber das Facing ist MovementDirection. In ClassicMMO SteerOrbit ist beides Camera-basiert. Die Trennung erlaubt beliebige Kombinationen.

---

## Neue Dateien

### 1. FacingMode.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/FacingMode.cs`

```csharp
namespace Wiesenwischer.GameKit.CharacterController.Core
{
    /// <summary>
    /// Bestimmt, was die Character-Rotation antreibt.
    /// </summary>
    public enum FacingMode
    {
        /// <summary>
        /// Character rotiert in Bewegungsrichtung (aktuelles Default-Verhalten).
        /// Funktioniert sowohl im Camera-Frame als auch Character-Frame.
        /// </summary>
        MovementDirection,

        /// <summary>
        /// Character alignt sich zur Kamera-Vorwärtsrichtung (ClassicMMO SteerOrbit).
        /// Character dreht sich auch ohne Bewegung zur Kamera.
        /// </summary>
        CameraForward,

        /// <summary>
        /// Character schaut zum Lock-On Target (Zukunft: Combat).
        /// </summary>
        TargetLockOn,

        /// <summary>
        /// Keine Facing-Änderung. Character behält aktuelle Rotation.
        /// </summary>
        None
    }
}
```

### 2. IOrientationProvider.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/IOrientationProvider.cs`

```csharp
using UnityEngine;

namespace Wiesenwischer.GameKit.CharacterController.Core
{
    /// <summary>
    /// Liefert den Referenzraum für Movement-Input-Interpretation.
    /// Implementierungen: CameraOrientationProvider (Camera-Package),
    /// VehicleOrientationProvider (Mount-Package, Zukunft).
    /// </summary>
    public interface IOrientationProvider
    {
        /// <summary>
        /// Forward-Richtung für Movement-Input.
        /// Camera-Frame: Camera Forward (Y=0, normalisiert).
        /// Character-Frame: Character Forward.
        /// </summary>
        Vector3 GetMovementForward();

        /// <summary>
        /// Right-Richtung für Movement-Input.
        /// </summary>
        Vector3 GetMovementRight();
    }
}
```

### 3. IFacingProvider.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/IFacingProvider.cs`

```csharp
using UnityEngine;

namespace Wiesenwischer.GameKit.CharacterController.Core
{
    /// <summary>
    /// Bestimmt, wie der Character rotiert/facing bestimmt wird.
    /// Getrennt von IOrientationProvider, da Facing und Frame-Space
    /// unabhängige Konzepte sind.
    /// </summary>
    public interface IFacingProvider
    {
        /// <summary>Aktueller Facing-Modus.</summary>
        FacingMode GetFacingMode();

        /// <summary>
        /// Zielrichtung für CameraForward/TargetLockOn Modi.
        /// Nur relevant wenn FacingMode != MovementDirection.
        /// </summary>
        Vector3 GetFacingDirection();
    }
}
```

---

## Bestehende Datei modifizieren

### LocomotionInput erweitern

**Pfad:** `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/Locomotion/ILocomotionController.cs`

Zwei neue Felder am **Ende** der `LocomotionInput`-Struct hinzufügen:

```csharp
public struct LocomotionInput
{
    // --- Bestehende Felder (NICHT ändern) ---
    public Vector2 MoveDirection;
    public Vector3 LookDirection;
    public bool StepDetectionEnabled;
    public float SpeedModifier;
    public float DecelerationOverride;

    // --- Phase 29: Neue Felder ---

    /// <summary>
    /// Wie soll der Character rotieren?
    /// Default (0 = MovementDirection) = bestehendes Verhalten.
    /// </summary>
    public FacingMode FacingMode;

    /// <summary>
    /// Zielrichtung für CameraForward/TargetLockOn Modi.
    /// Nur relevant wenn FacingMode != MovementDirection.
    /// </summary>
    public Vector3 FacingDirection;
}
```

**Rückwärtskompatibilität:** `FacingMode` hat Default-Wert `0` = `MovementDirection`. Bestehender Code, der `LocomotionInput` erstellt ohne die neuen Felder zu setzen, bekommt automatisch das bisherige Verhalten.

---

## Anweisungen

### Schritt 1: FacingMode.cs erstellen

1. Erstelle `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/FacingMode.cs`
2. Namespace: `Wiesenwischer.GameKit.CharacterController.Core`
3. Enum exakt wie oben mit XML-Docs

### Schritt 2: IOrientationProvider.cs erstellen

1. Erstelle `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/IOrientationProvider.cs`
2. Namespace: `Wiesenwischer.GameKit.CharacterController.Core`
3. `using UnityEngine;` nicht vergessen

### Schritt 3: IFacingProvider.cs erstellen

1. Erstelle `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/IFacingProvider.cs`
2. Namespace: `Wiesenwischer.GameKit.CharacterController.Core`
3. Referenziert `FacingMode` (gleicher Namespace, kein extra Using nötig)

### Schritt 4: LocomotionInput erweitern

1. Öffne `ILocomotionController.cs`
2. Füge `using Wiesenwischer.GameKit.CharacterController.Core;` hinzu (falls nicht schon vorhanden, für FacingMode)
3. Füge `FacingMode` und `FacingDirection` Felder ans Ende der Struct
4. XML-Docs für beide Felder

**Wichtig:** Bestehende Felder NICHT ändern. Nur neue Felder hinzufügen.

---

## Verifikations-Checkliste

- [ ] `FacingMode.cs` existiert in `CC.Core/Runtime/Core/`
- [ ] Enum hat 4 Werte: `MovementDirection`, `CameraForward`, `TargetLockOn`, `None`
- [ ] `IOrientationProvider.cs` existiert in `CC.Core/Runtime/Core/`
- [ ] Interface hat `GetMovementForward()` und `GetMovementRight()`
- [ ] `IFacingProvider.cs` existiert in `CC.Core/Runtime/Core/`
- [ ] Interface hat `GetFacingMode()` und `GetFacingDirection()`
- [ ] `LocomotionInput` hat neue Felder `FacingMode` und `FacingDirection`
- [ ] Default-Wert von `FacingMode` (0) = `MovementDirection`
- [ ] Bestehende LocomotionInput-Felder unverändert
- [ ] Projekt kompiliert fehlerfrei
- [ ] Bestehende Tests laufen weiterhin grün

---

## Erwartete Dateien nach diesem Schritt

```
Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/
├── FacingMode.cs                (NEU)
├── IOrientationProvider.cs      (NEU)
├── IFacingProvider.cs           (NEU)
├── ICameraOrbitProvider.cs      (unverändert, bleibt für Legacy)
├── PlayerController.cs          (unverändert)
└── Locomotion/
    ├── ILocomotionController.cs (ERWEITERT — LocomotionInput + 2 Felder)
    └── CharacterLocomotion.cs   (unverändert)
```

---

## Nächster Schritt

→ [29.2 ICameraInputStrategy + Pipeline Refactoring](29.2-input-strategy-refactoring.md)
