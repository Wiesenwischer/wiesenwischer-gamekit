# 29.7 Camera-Relative Animation Space (Vorbereitung)

> **Commit:** `feat(phase-29): 29.7 AnimationSpaceConverter (Vorbereitung)`
> **Branch:** `feat/animation-space` (vom Integration-Branch)

---

## Ziel

Grundlage für **zukünftiges Strafing** vorbereiten: Ein `AnimationSpaceConverter` der World-Move-Direction in character-lokale MoveX/MoveZ Animator-Parameter umrechnet.

**Wichtig:** Aktuell wird nur der Converter erstellt und getestet. Die Animator-Parameter (MoveX, MoveZ) werden **nicht** aktiviert — das geschieht erst wenn Strafe-Animationen vorhanden sind. Der bestehende 1D-Speed-Blend-Tree bleibt aktiv.

---

## Neue Dateien

### AnimationSpaceConverter.cs

**Pfad:** `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/Animation/AnimationSpaceConverter.cs`

```csharp
using UnityEngine;

namespace Wiesenwischer.GameKit.CharacterController.Core
{
    /// <summary>
    /// Konvertiert World-Move-Direction in Character-lokale Richtung
    /// für Animator-Parameter (MoveX/MoveZ).
    /// Statische Utility-Klasse — zustandslos, rein mathematisch.
    /// </summary>
    public static class AnimationSpaceConverter
    {
        /// <summary>
        /// Konvertiert eine Welt-Bewegungsrichtung in character-lokale Koordinaten.
        /// Ergebnis: x = Strafe (positiv = rechts), z = Forward/Backward (positiv = vorwärts).
        /// </summary>
        /// <param name="worldMoveDir">Normalisierte Bewegungsrichtung in Weltkoordinaten.</param>
        /// <param name="characterTransform">Transform des Characters für lokale Konvertierung.</param>
        /// <returns>Lokale Richtung: x = strafe, z = forward.</returns>
        public static Vector3 WorldToLocal(Vector3 worldMoveDir, Transform characterTransform)
        {
            if (worldMoveDir.sqrMagnitude < 0.001f)
                return Vector3.zero;

            return characterTransform.InverseTransformDirection(worldMoveDir);
        }

        /// <summary>
        /// Berechnet den signierten Winkel zwischen Character-Forward und Bewegungsrichtung.
        /// Positiv = Drehung nach rechts, Negativ = nach links.
        /// Nützlich für Turn-Animationen.
        /// </summary>
        /// <param name="worldMoveDir">Normalisierte Bewegungsrichtung in Weltkoordinaten.</param>
        /// <param name="characterForward">Forward-Richtung des Characters.</param>
        /// <returns>Signierter Winkel in Grad (-180 bis 180).</returns>
        public static float GetTurnAngle(Vector3 worldMoveDir, Vector3 characterForward)
        {
            if (worldMoveDir.sqrMagnitude < 0.001f)
                return 0f;

            Vector3 flatMove = new Vector3(worldMoveDir.x, 0, worldMoveDir.z).normalized;
            Vector3 flatForward = new Vector3(characterForward.x, 0, characterForward.z).normalized;

            return Vector3.SignedAngle(flatForward, flatMove, Vector3.up);
        }
    }
}
```

---

## Zukünftige Verwendung (NICHT in dieser Phase)

### Animator-Parameter

| Parameter | Quelle | Verwendung |
|-----------|--------|------------|
| `MoveX` | `localMove.x` | Strafe Left/Right |
| `MoveZ` | `localMove.z` | Forward/Backward |
| `Speed` | `localMove.magnitude` | Blend Tree Speed (besteht) |
| `TurnAngle` | `GetTurnAngle()` | Turn-Animationen |

### Integration in AnimationParameterBridge (Zukunft)

```csharp
// Zukunft: In AnimationParameterBridge.UpdateParameters()
Vector3 localMove = AnimationSpaceConverter.WorldToLocal(worldVelocity, character);
animator.SetFloat("MoveX", localMove.x);
animator.SetFloat("MoveZ", localMove.z);
float turnAngle = AnimationSpaceConverter.GetTurnAngle(worldVelocity, character.forward);
animator.SetFloat("TurnAngle", turnAngle);
```

---

## Anweisungen

### Schritt 1: Ordner erstellen

1. Erstelle `Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/Animation/` Ordner falls nötig

### Schritt 2: AnimationSpaceConverter.cs erstellen

1. Statische Klasse mit zwei Methoden
2. `WorldToLocal()` — InverseTransformDirection
3. `GetTurnAngle()` — SignedAngle zwischen Forward und MoveDir
4. Null-Checks / Zero-Magnitude-Guards

### Schritt 3: Keine Integration

**Nicht** in bestehenden Code integrieren. Nur Utility erstellen + testen (in 29.8).

---

## Verifikations-Checkliste

- [ ] `AnimationSpaceConverter.cs` existiert in `CC.Core/Runtime/Core/Animation/`
- [ ] `WorldToLocal()` konvertiert korrekt
- [ ] `GetTurnAngle()` berechnet signierten Winkel
- [ ] Zero-Magnitude Guard vorhanden
- [ ] Keine Integration in bestehenden Code (nur Utility)
- [ ] Projekt kompiliert fehlerfrei

---

## Erwartete Dateien nach diesem Schritt

```
Packages/Wiesenwischer.GameKit.CharacterController.Core/Runtime/Core/
└── Animation/
    └── AnimationSpaceConverter.cs    (NEU)
```

---

## Nächster Schritt

→ [29.8 Unit Tests](29.8-unit-tests.md)
