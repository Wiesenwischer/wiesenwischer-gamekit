# 27.1 Package-Struktur + OrbitBehaviour

> **Commit:** `feat(phase-27): 27.1 Package-Struktur + OrbitBehaviour`

---

## Ziel

Neues Package `Wiesenwischer.GameKit.Camera.Behaviours` anlegen und das erste Behaviour implementieren: **OrbitBehaviour** — extrahiert die Yaw/Pitch-Logik aus dem CameraBrain.

---

## Dateien

### 1. Package-Konfiguration

**`Packages/Wiesenwischer.GameKit.Camera.Behaviours/package.json`**
```json
{
  "name": "wiesenwischer.gamekit.camera.behaviours",
  "displayName": "GameKit Camera Behaviours",
  "version": "0.1.0",
  "unity": "2022.3",
  "description": "Modulare Camera Behaviours — Orbit, Zoom, Collision, Inertia, Recenter, ShoulderOffset.",
  "dependencies": {
    "wiesenwischer.gamekit.camera.core": "0.1.0"
  }
}
```

### 2. Assembly Definition

**`Runtime/Wiesenwischer.GameKit.Camera.Behaviours.Runtime.asmdef`**
```json
{
  "name": "Wiesenwischer.GameKit.Camera.Behaviours.Runtime",
  "rootNamespace": "Wiesenwischer.GameKit.Camera.Behaviours",
  "references": [
    "Wiesenwischer.GameKit.Camera.Core.Runtime"
  ],
  "includePlatforms": [],
  "excludePlatforms": []
}
```

### 3. OrbitBehaviour

**`Runtime/OrbitBehaviour.cs`**

Extrahiert die Orbit-Logik (Yaw/Pitch) aus `CameraBrain.UpdateOrbit()`.

```csharp
namespace Wiesenwischer.GameKit.Camera.Behaviours
{
    /// <summary>
    /// Orbit-Verhalten: Wendet Look-Input als Yaw/Pitch-Rotation an.
    /// </summary>
    public class OrbitBehaviour : MonoBehaviour, ICameraBehaviour
    {
        [Header("Pitch Limits")]
        [SerializeField] private float _minPitch = -40f;
        [SerializeField] private float _maxPitch = 70f;

        public bool IsActive => enabled;

        public void UpdateState(ref CameraState state, CameraContext ctx)
        {
            state.Yaw += ctx.Input.LookX;
            state.Pitch -= ctx.Input.LookY;
            state.Pitch = Mathf.Clamp(state.Pitch, _minPitch, _maxPitch);
        }
    }
}
```

**Vergleich mit CameraBrain.UpdateOrbit():**
- Gleiche Logik, aber Pitch-Limits als eigene SerializeFields statt aus CameraCoreConfig
- CameraBrain-Embedded: `_config.MinVerticalAngle` / `_config.MaxVerticalAngle`
- OrbitBehaviour: `_minPitch` / `_maxPitch` direkt auf der Komponente

---

## Akzeptanzkriterien

- [ ] Package kompiliert fehlerfrei
- [ ] OrbitBehaviour implementiert ICameraBehaviour
- [ ] Yaw addiert LookX, Pitch subtrahiert LookY
- [ ] Pitch wird auf Min/Max geclampt
- [ ] Namespace: `Wiesenwischer.GameKit.Camera.Behaviours`
